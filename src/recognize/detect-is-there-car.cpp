#include "detect-is-there-car.h"

vector_car_pos detectIsThereCar(recog_arg *arg) {
    vector_car_pos ret;

    ret.left   = false;
    ret.center = false;
    ret.right  = false;

    // 함수 작성
    return ret;
}